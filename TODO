- Needs Testing for Completion -

- Add RSS Feed || "Complete", although it needs solid testing
- Add Twitter Feed || Somewhat Working , needs further testing
- Add Permissions System through DB for Commands || Needs further testing and documentation


- Ongoing -
- Commands Documentation || In progress


- Priority List -

- Add user and group based permissions to registry checks || #1 priority
- Convert commands to use registry || Ongoing
- Configurable Message Channels || Plan laid out, needs implementing
- Logger || will depend on configurable message channels
- Channel Blacklist || This depends on the permissions system and message channels


- Upcoming Features -

- Add Message Formatter
- Add forum integrations || new topics are almost working, need to add more robust features, like subscribing specific subforums to channels
